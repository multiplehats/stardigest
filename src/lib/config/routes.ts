// ROUTE IDS (Route IDS can also look like (app)/dashboard that's)
export const APP_ROUTE = '/app' as const;
export const SIGNIN_ROUTE = '/auth' as const;

// PATHS
export const AUTH_PATH = '/auth' as const;
export const SIGNIN_PATH = '/auth' as const;
